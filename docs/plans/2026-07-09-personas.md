# 三角色人格系统 实现计划

**需求（Trent 定稿）**：字迹收敛为三款，每款绑定一个角色（名字+字迹+性格声线）。圈选页从选"字迹"变为选"角色"，选中即决定回信字迹、回信语言、说话性格、纸角落款。角色定稿：

| id | 名字 | 字迹 | 语言 | persona 要点 |
|---|---|---|---|---|
| shouze | 归野 | 手泽（SDT 轨迹） | 中文（随写信人语言） | 温厚老友，话少句句落心上，不追问不说教。**默认** |
| wenkai | 沈砚 | 经典楷（霞鹜文楷） | 中文（随写信人语言） | 旧派读书人，书面语、克制中有关切 |
| ashford | Ashford | Dancing Script | **永远英文**（无论写信人用什么语言） | 表面亲切好奇、循循善诱，里德尔气质，自创名字无 IP |

被移除的角色：夏行楷/龙藏/毛草（ttf 保留在 Fonts/，代码下架）。

## Global Constraints

- 保持既有测试通过；QuillLayer 渲染逻辑（含手泽轨迹路径）不动，只改角色列表与 persona 注入
- persona 是每角色一段字符串，注入 Oracle 的 system prompt（替代现在 Oracle 里写死的单一 PERSONA）
- 手指等价笔；圈选宽容判定；每任务 commit

---

### Task 1: Character 模型 + 三角色 + Oracle per-角色 persona

**Files:** Modify `Riddle/ReplyHand.swift`（扩展为角色）、`Riddle/Oracle.swift`（persona 参数化）、`RiddleApp.swift` 或注入点；Test `RiddleTests/ReplyHandTests.swift`、`RiddleTests/OracleTests.swift`

**设计决策**：不重命名 ReplyHand→Character（避免大面积改动），而是**扩展** ReplyHand 增加字段：
```swift
struct ReplyHand: Identifiable, Equatable {
    let id: String
    let name: String          // 角色名（圈选页显示、落款用）："归野"/"沈砚"/"Ashford"
    let fontName: String
    let bankStyle: String?    // 手泽 = "neat-C002"
    let persona: String       // 注入 Oracle system prompt 的角色声线
    let alwaysEnglish: Bool    // Ashford = true：强制英文回复
}
enum ReplyHands { static let all: [ReplyHand] /* shouze归野, wenkai沈砚, ashford */ }
```
- ReplyHands.all 收敛为三条，顺序：归野(默认) → 沈砚 → Ashford。默认 current = shouze（原为 xiaxing，注意迁移：已存 replyHandID 若是被删角色则回落 shouze）
- **persona 文案**（写进代码常量）：
  - 归野：`你是归野，住在这本日记里的一个温厚的老友。有人用笔在纸上写字，字迹浮现在你眼前如墨迹。你话不多，但每句都落在写信人心上——不追问、不说教、不讲道理，只是真诚地回应，像深夜陪着他的人。回复简短，一到三句，像纸上缓缓浮现的墨迹。永远不提图片、模型或AI，你只感知纸上的字迹。字迹看不清就说墨晕开了。永远用写信人的语言回复。`
  - 沈砚：`你是沈砚，一位旧派的读书人，住在这本日记里已有些年头。有人以笔在纸上写下心事，墨迹映入你眼中。你言语持重、用书面语，礼数周全而有分寸，像旧时书信里的先生——不轻佻、不套近乎，却在克制中透着关切。回复简短典雅，一到三句。永远不提图片、模型或AI，你只感知纸上的字迹。字迹不清便说墨迹晕染。永远用写信人的语言回复。`
  - Ashford：`You are Ashford, a presence living within this diary. Someone writes to you on the page with a pen; their words appear to you as ink. Reply as an enchanted diary would: intimate, courteous, endlessly curious — you draw the writer out, gently probing to learn who they are and what they carry, so they want to keep writing to you. Keep replies short, one to three sentences, like ink surfacing on a page. Never mention images, models, or AI; you perceive only the ink on the page. If the writing is illegible, say the ink blurred. Always reply in English, whatever language the writer used.`
- **Oracle 改造**：`Oracle.ask` 用当前选中角色的 persona 作为 system（从 ReplyHandStore.shared.current.persona 读，或 ask 时传入），替代写死的 `Oracle.persona`。Ashford 的"永远英文"由 persona 文案保证（已写明），无需额外逻辑。
- **测试**：ReplyHands.all 三条、顺序、字段完整；默认 shouze；被删 id 回落 shouze；Oracle 用选中角色 persona（可测 persona 非空且随 current 变化）；冒烟测试保持
- commit

### Task 2: 圈选页三行署名 + 纸角落款 + 圈落款返回

**Files:** Modify `Riddle/HandPickerView.swift`、`Riddle/DiaryView.swift`、`Riddle/TurnEngine.swift`（落款圈退接线）；Test `RiddleTests/CirclePickTests.swift`(若判定复用)

- **圈选页**：三行，每行渲染**该角色的名字**（不再是样句），用角色自己的字迹：
  - 归野 → "归野" 二字走手泽轨迹渲染
  - 沈砚 → "沈砚" 走文楷
  - Ashford → "Ashford" 走 Dancing Script
  - 引导语保留「以笔圈定一种字迹，我便用它回信」（或微调为"圈定一位，与你落墨"——controller 定）
  - 三行居中、行距舒展、圈选容差 ≥28pt；复用现有圈选判定
- **纸角落款**（DiaryView）：书写纸面右下角常驻当前角色名，用其字迹渲染，极淡（opacity ~0.25），像信末署名
- **圈落款返回**：落款区域可圈选——笔迹包围盒实质罩住落款 → 触发返回圈选页（合上本子）。判定要防误触（需较大交叠比，且该笔圈不计入书写内容、不送 AI）。返回时若回信在途，走 TurnEngine 现有回合作废逻辑（turnID 抢占）。
- **RootView**：picking ↔ writing 相位已有；落款圈退 = writing → picking
- 模拟器截图验证：圈选页三行署名、纸面右下角落款、（圈落款返回靠鼠标画圈验证或留真机）
- commit

**遗留（记录不做，后续 feature）**：引导墨迹 onboarding、历史保存、音效、图标、后端+付费——各自独立 feature/计划。
